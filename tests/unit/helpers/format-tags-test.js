
import { formatTags } from 'task-manager/helpers/format-tags';
import { module, test } from 'qunit';

module('Unit | Helper | format tags');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTags([42]);
  assert.ok(result);
});

