
import { cleanString } from 'task-manager/helpers/clean-string';
import { module, test } from 'qunit';

module('Unit | Helper | clean string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cleanString([42]);
  assert.ok(result);
});

