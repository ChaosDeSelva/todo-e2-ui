import shareme from 'task-manager/utils/shareme';
import { module, test } from 'qunit';

module('Unit | Utility | shareme');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shareme();
  assert.ok(result);
});
